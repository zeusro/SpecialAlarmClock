<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/menu_item_layout"
    android:layout_width="match_parent"
    android:layout_height="38dp" >

    <ImageView
        android:id="@+id/menu_item_next"
        android:layout_width="10dp"
        android:layout_height="16dp"
        android:layout_centerVertical="true"
        android:contentDescription="@string/empty" 
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"/>

    <ImageView
        android:id="@+id/menu_item_img"
        android:layout_width="10dp"
        android:layout_height="25dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/empty"
        android:padding="1dp"
        android:src="@mipmap/ic_launcher"
        android:visibility="invisible"
         />

    <TextView
        android:id="@+id/menu_item_tv"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:layout_centerVertical="true"
        android:layout_marginLeft="1dp"
        android:layout_toLeftOf="@+id/menu_item_next"
        android:layout_toRightOf="@+id/menu_item_img"
        android:gravity="center_vertical"
        android:text="@string/app_name"
        android:textSize="16dp" 
        android:singleLine="true"/>

    <TextView
        android:id="@+id/menu_h_line"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_alignParentBottom="true"
        android:background="@color/color_line"
        android:text="@string/app_name" 
        />

</RelativeLayout>
